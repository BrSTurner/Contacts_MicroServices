apiVersion: v1
kind: Service
metadata:
  name: inquiry-application-service
spec:
  type: ClusterIP
  selector:
    app: inquiry-application
  ports:
    - protocol: TCP
      port: 5001
      targetPort: 5001