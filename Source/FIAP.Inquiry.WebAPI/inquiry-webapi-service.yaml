apiVersion: v1
kind: Service
metadata:
  name: inquiry-webapi-service
spec:
  type: LoadBalancer  # Ou NodePort, dependendo do ambiente
  selector:
    app: inquiry-webapi
  ports:
    - port: 80
      targetPort: 80
