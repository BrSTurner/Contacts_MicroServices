apiVersion: v1
kind: Service
metadata:
  name: inquiry-webapi-service
  labels:     
    monitored: "true"
spec:
  type: LoadBalancer  # Ou NodePort, dependendo do ambiente
  selector:
    app: inquiry-webapi
  ports:
    - name: metrics
      port: 82
      targetPort: 8081
