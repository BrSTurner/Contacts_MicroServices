apiVersion: v1
kind: Service
metadata:
  name: registration-application-service
spec:
  type: ClusterIP
  selector:
    app: registration-application
  ports:
    - protocol: TCP
      port: 5001
      targetPort: 5001